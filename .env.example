PORT=3000

# Optional: store runtime data outside the repo or on a mounted persistent disk.
# DATA_DIR=/var/data
# DATA_FILE=/var/data/class-record.json

# Keep this false/empty in production. Set to true only for local demos.
ALLOW_DATA_RESET=false
